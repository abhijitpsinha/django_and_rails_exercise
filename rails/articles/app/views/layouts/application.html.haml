!!!
%html{lang: "en"}
  %head
    %meta{ name: 'keywords', content: 'articles, gale' } 
    %meta{ name: 'description', content: 'An exercise on articles.' }
    %meta{charset: "utf-8"}
      %title
        Articles
      = stylesheet_link_tag 'application'
      = javascript_include_tag 'application'
    = csrf_meta_tag
%body
  #wrapper
    = render 'shared/navigation_links'
    .content-wrapper
      .detail_view
        = yield
      = render 'shared/next_reads'
